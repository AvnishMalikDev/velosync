# Canonical env is one level up: Product/.env
#
# Copy the template from the Product folder:
#   copy ..\.env.example ..\.env
#   macOS/Linux: cp ../.env.example ../.env
#
# If you still have jira-md-export/.env only, the server migrates it to Product/.env on startup.
